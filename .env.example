# Optional. The app works without Claude and never needs a key for FBA/FVA.
ANTHROPIC_API_KEY=

# Override only if your Anthropic account uses a different supported model.
ANTHROPIC_MODEL=claude-sonnet-5
